<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BROADCAST_NOTIFICATION" />
    <uses-permission android:name="android.permission.VOLUME_UP" />
    <uses-permission android:name="android.permission.VOLUME_DOWN"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>



    <application
        android:name=".Owl"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/test"
        android:label="@string/app_name"
        android:roundIcon="@drawable/test"
        android:supportsRtl="true"
        android:theme="@style/Theme.OWL"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">
        <activity
            android:name=".Lecture"
            android:exported="false" />
        <activity
            android:name=".MainListener"
            android:exported="false"
            android:label="@string/title_activity_main_listener" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Déclaration du BroadcastReceiver -->
        <receiver android:name=".Tools.NotificationActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="action_previous" />
                <action android:name="action_play" />
                <action android:name="action_next" />
            </intent-filter>
        </receiver>
        <service
            android:name=".Tools.NotificationService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".Tasks.ImageService"
            android:exported="false"/>
    </application>

</manifest>